{
  "name": "nodejs-simple-app",
  "version": "1.0.0",
  "description": "Simple web server that shows an index page with EC2 instance id",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aniruddha Wani",
  "license": "ISC",
  "dependencies": {
    "request": "^2.85.0"
  }
}
